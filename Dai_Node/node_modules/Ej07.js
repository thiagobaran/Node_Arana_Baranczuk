const countryCurrencyMap = require('currency-map-country');

export function obtenerMoneda(codigoPais) {
  const moneda = countryCurrencyMap.getCurrencyByCountryCode(codigoPais);
  return moneda || null;
}